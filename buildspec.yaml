version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
  build:
    commands:
      - echo Building the code...
  post_build:
    commands:
      - echo Copying deployShift.yaml to root directory.
      - ls -la
artifacts:
  files:
    - deployShift.yaml
